// @flow

// leave off @2x/@3x
const images = {
  // didn't get what's the difference between logo & clearLogo, but let's move on first
  logo: require('../Images/logo.png'),
  clearLogo: require('../Images/top_logo.png')
}

export default images
